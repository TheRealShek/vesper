[docs/01_Vision.md] [4. Explicitly Accepted Constraints] — Directory symlinks are not followed in v1 vs Directory symlinks are followed one level deep — src/index/walker.rs:208-214
[docs/01_Vision.md] [4. Explicitly Accepted Constraints] — Overlapping source roots are rejected vs Overlapping/nested roots are not checked or rejected during addition — src/backend/app_loop.rs:55-58
[docs/01_Vision.md] [2. Core Philosophy and Non-Goals] — Tag identity is path-qualified to prevent merging vs Tags are global unique strings, causing duplicate folder names to merge — src/db/schema.rs:61-64
[docs/01_Vision.md] [4. Explicitly Accepted Constraints] — Displayed tag names reflect folder names exactly and duplicate names are disambiguated by lineage vs Lineage disambiguation is not implemented in the UI — src/ui/window.rs:414-428
[docs/01_Vision.md] [4. Explicitly Accepted Constraints] — Dates come from reliable filesystem/application metadata, preferring Date added where birth time cannot be guaranteed vs Sorting uses created_at (birth time) instead of indexed_at (Date added) — src/db/search.rs:102-111
[docs/02_Architecture.md] [1. Source Directory Model] — File system events are debounced for 300ms before processing vs Debounce time is set to 500ms — src/config.rs:5
[docs/02_Architecture.md] [1. Source Directory Model] — Directory symlinks are not followed vs Directory symlinks are followed one level deep — src/index/walker.rs:208-214
[docs/02_Architecture.md] [1. Source Directory Model] — A root is rejected if its canonical path duplicates, is inside, or contains an existing root vs Overlapping/nested roots are not checked or rejected — src/backend/app_loop.rs:55-58
[docs/02_Architecture.md] [3. Tag Model and Tag Behavior] — Tag identity is path-qualified to prevent merging vs Tags are global unique strings, causing duplicate folder names to merge — src/db/schema.rs:61-64
[docs/02_Architecture.md] [3. Tag Model and Tag Behavior] — If multiple tags share the same display name, the sidebar row must disambiguate them vs Lineage disambiguation is not implemented in the UI — src/ui/window.rs:414-428
[docs/02_Architecture.md] [4. Storage, Index, and Cache Model] — All schema changes go through explicit migrations; startup must not rely on best-effort ALTER TABLE statements vs initialize runs best-effort ALTER TABLE commands on startup — src/db/schema.rs:13-17
[docs/02_Architecture.md] [4. Storage, Index, and Cache Model] — Database schema must contain tables schema_migrations, scan_errors, settings, and session_state vs These tables are not created or used — src/db/schema.rs:34-79
[docs/02_Architecture.md] [4. Storage, Index, and Cache Model] — media table contains columns relative_path, canonical_identity, date_added, thumbnail_cache_key, thumbnail stale/failure status, last_accessed_at vs media table lacks these columns — src/db/schema.rs:43-58
[docs/02_Architecture.md] [4. Storage, Index, and Cache Model] — Session state, settings, ignore rules, and root-as-tag are stored in SQLite settings/session_state tables vs Session and settings state are persisted in state.json — src/state.rs:71-98
[docs/02_Architecture.md] [8. Session Persistence Behavior] — Scroll position is persisted/restored as a stable anchor: anchor_media_id/path, anchor_offset_within_cell, sort/filter context hash vs Scroll position is persisted as a raw item index — src/state.rs:11
[docs/02_Architecture.md] [9. WIDGET TREE (source of truth)] — viewer_overlay is mounted on a top-level app_overlay (directly under ApplicationWindow) vs Viewer is mounted on main_overlay inside grid_overlay, nested inside root_stack — src/ui/window.rs:771-778
[docs/02_Architecture.md] [5. Runtime and Background Work Model] — The app acquires a library lock before opening the database for write access vs Database is opened directly without obtaining any lock — src/main.rs:58-65
[docs/02_Architecture.md] [6. Transient Files and Partial Copies] — Read metadata twice with a short delay and retry unstable files with bounded backoff vs Metadata is read once with no delay/retry mechanism — src/backend/live_update.rs:107-126
[docs/03_Implementation.md] [1. SIDEBAR INTERNAL LAYOUT] — Only ScrolledWindow gets vexpand=true in sidebar vs sidebar_root Box also has vexpand=true — src/ui/sidebar.rs:45
[docs/03_Implementation.md] [2. HEADER BAR LAYOUT] — Zoom slider widget has width-request=100 and 5 tick marks with labels (XS, S, M, L, XL) vs Zoom slider has width-request=120 and no tick marks or labels — src/ui/header.rs:105-111
[docs/03_Implementation.md] [3. CSS RULES (critical)] — Grid cell templates must include a .selected-tint overlay vs No .selected-tint widget is defined or styled — src/ui/grid_cell.rs:24-91
[docs/03_Implementation.md] [3. CSS RULES (critical)] — Contains CSS rules for .filter-pill vs .filter-pill class is not present in style.css — src/ui/style.css:1-257
[docs/03_Implementation.md] [4. TOP-LEVEL OVERLAY PLACEMENT] — Mount viewer_overlay on app_overlay, not inside grid-only overlay vs Viewer is mounted on main_overlay inside grid_overlay — src/ui/window.rs:771-778
[docs/03_Implementation.md] [5. SETTINGS DIALOG LAYOUT] — Settings window has a Library Maintenance group with Rescan Library, Regenerate Thumbnails, Rebuild Library Index buttons vs No Library Maintenance group or buttons exist — src/ui/settings.rs:191-225
[docs/03_Implementation.md] [5. SETTINGS DIALOG LAYOUT] — Settings window has a Restore Default Ignore Rules button vs No Restore Default Ignore Rules button exists — src/ui/settings.rs:138-190
[docs/04_Product_Spec.md] [1. Search Behavior] — Search results are ranked in an 8-tier hierarchy vs Results are ranked in 3 tiers, using m.id instead of path as final tie-breaker — src/db/search.rs:102-129
[docs/04_Product_Spec.md] [3. Sidebar Behavior] — Folder lineage is provided to disambiguate tag display names vs Disambiguation is not implemented in the UI — src/ui/window.rs:414-428
[docs/04_Product_Spec.md] [5. Grid Cell Specification] — Zoom level has 5 steps: XS, S, M, L, XL vs Zoom slider has no labels/steps configured, and uses continuous float scale values — src/ui/header.rs:105-111
[docs/04_Product_Spec.md] [6. Image Behavior] — Maximum zoom is 800% and changes in 12.5% relative steps vs Scroll step is 15% (1.15) and there is no maximum zoom clamp — src/ui/viewer.rs:913-918
[docs/04_Product_Spec.md] [8. Viewer Overlay Behavior] — Info panel displays date added, modified date vs Displays Created and Modified dates, and no Date added field — src/ui/viewer.rs:288-289
[docs/04_Product_Spec.md] [8. Viewer Overlay Behavior] — Info panel slides in and pushes the media layout/shrinks the media area vs Info panel is added as an overlay and floats on top of the media area — src/ui/viewer.rs:297
[docs/04_Product_Spec.md] [8. Viewer Overlay Behavior] — Wrap-around triggers a brief visual pulse/flash transition on the viewer vs Next/prev wrapping has no visual pulse/flash transition — src/ui/viewer.rs:587-609
[docs/04_Product_Spec.md] [8. Viewer Overlay Behavior] — On close, the grid scrolls back to and highlights the cell. The highlight fades after 900ms vs Grabs focus and scrolls, but no highlight or fade is implemented — src/ui/window.rs:556-564
[docs/04_Product_Spec.md] [9. Selection and Multi-Selection Behavior] — Open Location button is disabled if the selection spans more than one folder, showing a tooltip explanation vs Button is never disabled on multi-folder selections; opens the first parent — src/ui/selection_bar.rs:74-89
[docs/04_Product_Spec.md] [11. Sorting Behavior] — Sort options include Date added (newest/oldest first) vs UI dropdown and queries use Date created (created_at birth time) instead of Date added (indexed_at) — src/ui/header.rs:54-63
[docs/04_Product_Spec.md] [24. Settings Dialog] — Settings dialog has Library Maintenance group with Rescan, Regenerate, Rebuild buttons and a Restore Default Ignore Rules button vs None of these maintenance buttons exist in settings — src/ui/settings.rs:138-225
